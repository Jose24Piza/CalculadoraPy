name: Calculadora CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"
    
    - name: Verificar archivos
      run: |
        echo "Archivos encontrados:"
        ls -la *.py
        test -f calculadora.py && echo "✅ calculadora.py existe"
        test -f app.py && echo "✅ app.py existe"
    
    - name: Pruebas unitarias (CON FALLO INTENCIONAL)
      run: |
        python -c "
        from calculadora import Calculator
        calc = Calculator()
        
        # Pruebas que pasan
        assert calc.sum(2, 3) == 5
        assert calc.subtract(10, 5) == 5
        assert calc.multiply(3, 4) == 12
        
        # ❌ TEST QUE FALLA INTENCIONALMENTE
        assert calc.divide(10, 2) == 3, '❌ Test fallido intencionalmente'
        
        print('✅ Todas las pruebas pasaron correctamente')
        "
    
    - name: Prueba de app.py
      run: |
        echo -e "10\n+\n5" | python app.py | grep -q "15.0"
        echo "✅ app.py funciona correctamente"